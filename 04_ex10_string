// string library

#include <stdio.h>
#include <string.h>

main(){
  
    char str[80] = "My name is david";
    char *dest = "This is the lsp class."
    char *src; 
    
// Convert string to int
    char num[] = "10";
    int j = atoi(num);
    printf("String to int : " , j);
  
// conver int to string
    int number = 11111;
    sprintf(src, "%d", i);
    
// length of string
    printf(strlen(str));
    
// copy
    strcpy(str, src);
    
// add 
    strcat(str, dest);
    
// Compare
// return 0 if s1 == s2 or s1 > s2
// (s1 - s2) return
    printf(strcmp(dest, str));
    

}
